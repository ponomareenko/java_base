# Тема 1: Функциональное программирование и лямбда-выражения

## Функциональное программирование

**Определение:**

Функциональное программирование — это подход к написанию кода, при котором программа строится вокруг функций, а не вокруг изменения состояния объектов.

В Java функциональное программирование используется частично: через лямбда-выражения, функциональные интерфейсы, Stream API и ссылки на методы.

**Пример:**

```java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);

List<Integer> squares = numbers.stream()
        .map(number -> number * number)
        .toList();

System.out.println(squares); // [1, 4, 9, 16, 25]
```

---

## Императивное программирование

**Определение:**

Императивное программирование — это стиль программирования, при котором разработчик подробно описывает, какие команды и в каком порядке должна выполнить программа.

Главная идея: мы говорим программе, **как именно** получить результат.

**Пример:**

```java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);
List<Integer> result = new ArrayList<>();

for (Integer number : numbers) {
    if (number % 2 == 0) {
        result.add(number * 2);
    }
}

System.out.println(result); // [4, 8]
```

---

## Декларативное программирование

**Определение:**

Декларативное программирование — это стиль программирования, при котором разработчик описывает, **что нужно получить**, а не расписывает каждый шаг выполнения.

В Java декларативный стиль часто встречается при работе со Stream API.

**Пример:**

```java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);

List<Integer> result = numbers.stream()
        .filter(number -> number % 2 == 0)
        .map(number -> number * 2)
        .toList();

System.out.println(result); // [4, 8]
```

---

## Побочный эффект

**Определение:**

Побочный эффект — это любое действие функции или метода, которое изменяет что-то за пределами самой функции.

К побочным эффектам относятся:

* изменение состояния объекта;
* изменение глобальных или внешних переменных;
* вывод в консоль;
* запись в файл;
* работа с базой данных;
* отправка HTTP-запросов;
* выбрасывание или обработка исключений.

В продакшене побочные эффекты не являются чем-то запрещённым. Без них программа была бы как кассир без кассы: вроде всё понимает, но ничего не делает. Но их нужно контролировать.

**Пример:**

```java
public class Counter {
    private int count = 0;

    public void increment() {
        count++; // побочный эффект: изменяется состояние объекта
    }
}
```

---

// Другие термины

---

# Практическое задание

## Задание 1

Перепиши анонимный класс через лямбда-выражение:

```java
Comparator<Integer> comparator = new Comparator<Integer>() {
    @Override
    public int compare(Integer first, Integer second) {
        return second.compareTo(first);
    }
};
```

---

## Задание 2

Объясни, что выведет код и почему:

```java
public class Main {
    private final String name = "Outer";

    public void run() {
        Runnable task = () -> System.out.println(this.name);

        task.run();
    }
}
```

---

## Задание 3

Найди ошибку:

```java
int counter = 0;

Runnable task = () -> System.out.println(counter);

counter++;
```

Напиши, почему Java не разрешает такой код.

---

## Задание 4

Перепиши код без изменения внешнего списка внутри лямбды:

```java
List<Integer> result = new ArrayList<>();

List.of(1, 2, 3, 4, 5, 6).forEach(number -> {
    if (number % 2 == 0) {
        result.add(number);
    }
});
```

---

## Задание 5

Создай `Runnable`, который выводит:

```java
Java is running
```

Запусти его через метод `run()`.

---

## Задание 6

Покажи пример ленивых вычислений в Stream API.

Нужно, чтобы в коде было видно, что `filter()` не выполняется до терминальной операции.

---

## Задание 7

Создай `Optional<String>` со значением `"java"`.

Через цепочку методов:

1. переведи строку в верхний регистр;
2. если значения нет, верни `"UNKNOWN"`.

Ожидаемый результат:

```java
JAVA
```

---

## Задание 8

Создай `Function<String, Integer>` через ссылку на метод `String::length`.

Проверь на строке:

```java
"developer"
```

Ожидаемый результат:

```java
9
```

---

## Задание 9

Создай `Supplier<List<String>>` через ссылку на конструктор `ArrayList::new`.

Создай список через `get()`, добавь в него 2 строки и выведи результат.

---

## Задание 10

Ответь письменно:

1. Чем лямбда отличается от анонимного класса?
2. Что значит `effectively final`?
3. Почему нельзя бездумно изменять внешний список внутри лямбды?
4. Чем промежуточная операция Stream отличается от терминальной?
5. Когда method reference лучше обычной лямбды?
